/* funcoes em  setas = ela recebe um valor e retorna algo */
/* arrow function = funcao mais curta e direta pela seta =>(porem tem que guardar ela em uma variavel)*/

const soma = (num1, num2) => {
    return num1 + num2;
}